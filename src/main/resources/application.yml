server:
  port: 8081

spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/new_test_user?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      username: root
      password: root
      initial-size: 1
      min-idle: 1
      max-active: 20

mybatis:
  mapper-locations: classpath:MybatisMapper/*.xml
  type-aliases-package: com.generator.dao

